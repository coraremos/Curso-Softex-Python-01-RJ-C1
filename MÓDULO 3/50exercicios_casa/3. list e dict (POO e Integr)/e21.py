'''Classe Base ItemInventario

Crie uma classe ItemInventario com atributos: 
nome (string), quantidade (int) e valor_unitario (float). 
Implemente um método valor_total().

class, __init__, função (método)
'''

class TemInventario:
    def __init__(self, nome:str, quantidade:int, valor_unitario:float):
        self.nome = nome
        self.quantidade = quantidade
        self.valor_unitario = valor_unitario

    def produtos(dict):
        

    def valor_total(quantidade):
        if quantidade > 0:
            return "Há {quantidade} itens desse produto no inventário."
        return 'Não há itens no inventário'

